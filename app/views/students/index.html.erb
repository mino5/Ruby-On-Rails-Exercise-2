<h1>Listing students</h1>

<table>

<% @students.each do |student| %>
  <tr>
    <td><%= student.name %></td>
   <!--  <td><%= student.pesel %></td> -->
   <!-- <td><%= student.semester %></td>  -->
    <td><%= link_to 'Show more', student %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New student', new_student_path %>
